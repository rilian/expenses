<h1>
  <%= link_to 'Edit', edit_expense_path(@expense), class: 'btn btn-primary' %>
  <%= @expense.name %>
</h1>

<table class="table">
  <thead>
  <tr>
    <th>Month</th>
    <th>Sum</th>
    <th>Description</th>
  </tr>
  </thead>
  <tbody>
  <% @expense.month_expenses.each do |month_expense| %>
      <tr>
        <td>
          <%= link_to month_expense.month.name, month_path(month_expense.month) %>
        </td>
        <td>
          <%= month_expense.sum_real %>
        </td>
        <td>
          <%= month_expense.desc %>
        </td>
      </tr>
  <% end %>
  </tbody>
  <thead>
  <tr>
    <th>Month</th>
    <th>Sum</th>
    <th>Description</th>
  </tr>
  </thead>
</table>
